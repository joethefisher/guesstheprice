node_modules/
.next/
out/
dist/
.env
.env.*
!.env.example
*.log
.DS_Store
prisma/*.db
prisma/*.db-journal
.vscode/
.idea/
next-env.d.ts
tsconfig.tsbuildinfo
.cache/

# Local DB backup dumps (pg_dump output)
supabase-backup-*.sql
*.dump

design_handoff_pricetag/

# Local working docs (focus-session plans, brainstorms, internal notes).
# Tracked here so they don't leak into the public repo. Each plans/*.md
# is a focused brief for an upcoming work session.
plans/
